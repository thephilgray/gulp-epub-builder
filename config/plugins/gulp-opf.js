// TODO: optionally use an existing opf file, parse, transform, and recompile

// const through = require("through2");
// const xmlJs = require('xml-js');

// module.exports = function() {

//   const compileOpf = (f) => {

//   }

//   return through.obj(function(file, encoding, callback) {
//     if (file.isBuffer()) {
//       return callback(null, compileOpf(file));
//     } else {
//       return callback();
//     }
//   });
// };
